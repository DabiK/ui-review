/**
 * Version reported by the `bridge.health` operation. Kept in sync with `package.json` by
 * `tests/bridge/bridge-version.test.ts` so a release cannot advertise a stale version.
 */
export const BRIDGE_VERSION = '0.1.0';
